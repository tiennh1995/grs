<div class="comment">
  <% user = comment.user %>
  <div class="comment-left">
    <%= image_tag user.avatar_url, class: "img-responsive
      comment-avatar" %>
  </div>

  <div class="comment-right">
    <div class="comment-header">
      <p class="nick-name"><%= link_to user.nick_name, user %></p>
      <small><%= l comment.created_at, format: :default %></small>
    </div>

    <div class="comment-body">
      <p><%= comment.content %></p>
    </div>

    <div class="comment-footer">
      <span class="reply">
        <i class="fa fa-reply" title="Reply this comment"
          aria-hidden="true"></i>
      </span>

      <span class="reply-comment-icon" data-id="<%= comment.id %>">
        <i class="fa fa-comments" aria-hidden="true"></i>
        <%= comment.reply_comments.size %>
      </span>
    </div>
    <div class="clearfix"></div>
    <div class="reply-comments reply-<%= comment.id %>">
      <hr>
      <% comment.reply_comments.each do |reply_comment| %>
        <%= render "reply_comments/reply_comment",
          reply_comment: reply_comment %>
      <% end %>
    </div>
  </div>
  <div class="clearfix"></div>
</div>
