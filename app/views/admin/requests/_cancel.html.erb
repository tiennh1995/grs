<%= form_tag [:admin, _request], method: :patch, remote: true,
  class: "form-status" do %>
  <%= hidden_field_tag "request[status]", :waiting, {} %>
  <%= submit_tag "UnCancel?",
    class: "btn cancel" %>
<% end %>

