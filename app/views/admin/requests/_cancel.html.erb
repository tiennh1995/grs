<%= form_for [:admin, _request], method: :patch, remote: true,
  class: "form-status" do |f| %>
  <%= f.hidden_field :status, value: :waiting %>
  <%= f.submit "UnCancel?", class: "btn cancel" %>
<% end %>
